import 'package:flutter/material.dart';

class ClockinRecordPage extends StatelessWidget {
  const ClockinRecordPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('打卡记录')),
      body: const Center(child: Text('打卡记录')),
    );
  }
}
